# NEXT.md — 일꾼 오더 파일

> 덮어쓰기 전용. 헤드가 여기에 「지금 할 것」을 적으면 일꾼(Antigravity) 이 읽는다.

## 상설 규약

`AGENTS.md` §3 을 그대로 따른다. 요약:
- 기존 파일 재작성 금지, 요청받은 부분만
- 삭제 > 추가 면 멈추고 보고
- `git add -A` 금지, `git push` 금지
- 지금 코드와 다르면 다르다고 보고
- 「판정 불가」 허용
- 근거는 `파일:줄번호`, 항목당 한 줄
- 이모지 금지
- 커밋 전 기계 관문 통과

## 지금 할 것

**과제 없음.** 사용자에게 세 가지 결정을 요청한 상태 (`STATUS.md` 참조). 답이 오면 헤드가 이 파일을 덮어쓴다.

## 이 과제가 바꿀 경로

TBD.

## 완료 확인 방법

TBD.
