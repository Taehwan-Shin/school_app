{
  "name": "@school-app/functions",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "test:emu": "firebase emulators:exec --project=demo-school --only auth,functions,firestore 'vitest run --config vitest.emu.config.ts'",
    "lint": "tsc --noEmit && eslint src tests"
  },
  "dependencies": {
    "@school-app/shared": "workspace:*",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "googleapis": "^176.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "@typescript-eslint/eslint-plugin": "^8.68.0",
    "@typescript-eslint/parser": "^8.68.0",
    "eslint": "^10.9.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": "20"
  }
}
