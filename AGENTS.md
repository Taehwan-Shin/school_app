# AGENTS.md — 규칙의 단일 원본

> **무엇을·왜**는 여기. **어떻게**는 `OPERATIONS.md`. 상태는 `STATUS.md`. 일지는 `project_notes.md`.
>
> 이 파일의 규칙 본문을 다른 파일에 복사하지 마라 — 사본은 반드시 낡는다. 다른 문서는 이 파일을 가리키기만 한다.

## 1. 이 저장소가 무엇을 하는가

Google Sheets 위에서 돌던 두 개의 Apps Script(계정관리·클래스룸관리) 를 파이어베이스 기반 웹앱으로 옮긴다.

- **관리자가 시트를 열지 않고** 웹 UI 에서 워크스페이스 계정·그룹·챗방·클래스룸을 만든다·바꾼다·지운다.
- 파이어베이스는 호스팅·DB·로그인·서버 함수를 맡는다.
- **실제 워크스페이스 조작은 서버 함수가 서비스 계정(도메인 위임)으로 Google Admin·Classroom·Chat API 를 부른다** — 이 부분은 파이어베이스로 대체 불가.

원본 스크립트: `RESEARCH/school-webapp/source/` (읽기 전용 참고). 기능 목록: `RESEARCH/school-webapp/FEATURES_CATALOG.md`.

## 2. 세 자리

| 자리 | 하는 일 | 부르는 사람 |
|---|---|---|
| **Claude Code_Honey** (헤드) | 방향·설계·판정, 위험한 일(보안·삭제·배포) 직접 | 사람이 말로 |
| **Antigravity** (일꾼) | 화면·기능·대량 수정 — 만드는 일 | 헤드가 `NEXT.md` 로 |
| **Codex** (감사) | 커밋을 읽고 「여기 이상하다」를 항목으로 | 헤드가 CLI 로 |

**원칙 한 줄: 자기 작업을 자기가 승인하지 않는다.**

## 3. 상설 규약 — 일꾼(Antigravity) 몫

지시서 머리에 붙여 두는 것. 매번 다시 쓰지 않는다.

1. **기존 파일을 재작성하지 마라.** 요청받은 부분만 고친다.
2. **삭제가 추가보다 많으면 멈추고 보고**.
3. 과제별로 커밋을 끊고, **`git add -A` 금지** — 자기가 바꾼 파일만 명시.
4. **`git push` 금지**.
5. 지시가 지금 코드와 다를 수 있다. 맞추지 말고 **다르다고 보고**.
6. 실행해야 아는 것은 **「판정 불가」로 두고** 무엇을 실행해야 아는지 적어라.
7. 근거는 **`파일:줄번호`** 로. 항목당 한 줄, **서술형 장문 금지**.
8. **이모지 금지** — 입력창에서 깨진다.
9. **커밋 전에 자기가 기계 관문을 통과시킨다** — 타입 검사·빌드·자가 테스트.
10. 사람이 못 하는 확인은 지시서에 넣지 마라. 사람이 봐야 하는 것은 「판정 불가」로 남긴다.

## 4. 상설 규약 — 감사(Codex) 몫 (다섯 줄)

1. **「판정 불가」를 허용한다** — 무리해서 통과/실패로 몰지 마라.
2. **설계를 정답으로 전제하지 마라** — 지금 코드가 다르면 다르다고 보고.
3. **근거는 `파일:줄번호`** — 근거 없는 단정은 그 자체가 실패.
4. **보고 형식 고정** — `[통과/실패/판정불가] 파일:줄 — 한 줄`.
5. **대상은 커밋 해시로 고정**. 작업 중인 트리 금지.

## 5. 한 사이클 (묻지 않고 진행)

```
사람이 방향 제시
  → 헤드 설계 → 감사가 설계 검토 → 헤드 반영
  → 헤드가 자기 몫(안전·구조 핵심) 직접 구현 + 자가 테스트 추가
  → NEXT.md 에 과제 등록 → 헤드가 일꾼 직접 호출 → 구현
  → 일꾼이 자기 손으로 기계 관문 통과 → 커밋·핸드오버
  → 프로세스 종료 = 완료 신호 → git log/git status 로 실물 대조
  → 기계 관문(타입·빌드·자가 테스트·삭제 검사) → 감사 파견
  → 「실패」 주장은 전건 재확인 / 「통과」는 표본
  → 합격 → 배포(푸시는 헤드 또는 사람) → 사람에게 눈 확인 시나리오 → 종결
  → 모든 단계를 project_notes.md, 열린 항목은 STATUS.md
```

## 6. 안전 경계

- **`git push` 는 헤드 또는 사람만.**
- **`gh` · `.env.local` · `rm -rf` 금지 (일꾼).**
- **비밀키는 감사에게 접근시키지 않는다** — 감사는 read-only + `< /dev/null`.
- **커밋은 만든 사람이 한다.** 헤드가 대행하지 않는다.

## 7. 팀 규모 (지금)

- **셋이 다 있다.** 「자기 작업 자기 승인 안 함」 원칙을 그대로 적용한다.
- Antigravity 또는 Codex 가 어느 날 못 돌면, 그날은 **비는 자리를 알고** 진행한다 (헤드+일꾼 모드, 헤드 단독 모드).
